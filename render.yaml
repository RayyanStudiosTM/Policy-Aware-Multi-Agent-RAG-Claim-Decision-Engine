services:
  - type: web
    name: aptino-claim-engine-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && python scripts/ingest_policy.py
    startCommand: uvicorn backend.api.main:app --host 0.0.0.0 --port $PORT
